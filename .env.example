# Optional: absolute path for enquiry JSON so rebuilds/uploads never wipe data.
# Example (Linux VPS): ENQUIRY_STORAGE_PATH=/var/lib/anantam/enquiries.json
# If unset, data is saved to ./storage/enquiries.json (gitignored).
# ENQUIRY_STORAGE_PATH=
